import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.astra.safetyapp',
  appName: 'astra-womens-safety-app',
  webDir: 'dist'
};

export default config;
